node_modules/
dist/
.astro/
.vercel/
.DS_Store
*.log

# local-only tooling and notes (kept on disk, not published)
screenshots/
scripts/
EDITING.md
HANDOFF.md
# editor/MCP config — can hold API keys, must never be published
.mcp.json
